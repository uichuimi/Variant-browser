import { DeviceWidthBreakpointDirective } from './device-width-breakpoint.directive';

describe('DeviceWidthBreakpointDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceWidthBreakpointDirective();
    expect(directive).toBeTruthy();
  });
});
